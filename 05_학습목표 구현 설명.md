
제시하신 **4가지 학습목표가 실제 코드에 모두 반영**되어 있습니다. 특히 이 프로그램은 **사용자 입력 → LLM → JSON 구조화 → 장소검색 API → 다시 LLM → 최종 리포트**라는 API 연계 흐름을 학습하기 좋은 구조입니다.

## 1. REST API의 요청/응답 구조와 GET/POST 차이

`travel_planner`에서 가장 명확한 REST API 호출 사례는 네이버 장소검색 부분입니다.

```python
params = {"query": f"{city} 맛집", "display": count}

res = requests.get(
    NAVER_API_URL,
    headers=headers,
    params=params,
    timeout=10
)
```

여기에는 REST API 요청의 핵심 요소가 들어 있습니다.

|구성|travel_planner 코드|의미|
|---|---|---|
|URL|`NAVER_API_URL`|어느 API 서버에 요청할 것인가|
|Method|`requests.get()`|어떤 방식으로 요청할 것인가|
|Header|`headers`|인증키 등 부가정보|
|Parameter|`params`|`"부산 맛집"` 같은 검색조건|
|Response|`res`|서버가 돌려준 응답|
|Status Code|`res.status_code`|성공/실패 상태|
|JSON Body|`res.json()`|서버가 돌려준 실제 데이터|

즉 프로그램은 다음과 같이 동작합니다.

**Python 프로그램 → HTTP GET 요청 → Naver API → HTTP 응답 → JSON → Python 데이터**

응답은 다음 코드에서 JSON으로 변환합니다.

```python
items = res.json().get("items", [])
```

### GET과 POST의 차이

현재 `search_restaurants()`는 **GET**을 사용합니다. 검색처럼 서버에서 정보를 **조회**하는 목적이기 때문입니다.

일반적으로 GET은 데이터를 조회할 때 사용하고, POST는 서버에 데이터를 보내 새로운 처리나 생성을 요청할 때 사용합니다.

예를 들어 개념적으로는:

```python
# GET : 맛집 정보를 조회
requests.get(url, params={"query": "부산 맛집"})

# POST : 데이터를 서버에 전달
requests.post(url, json={"city": "부산"})
```

다만 `travel_planner.py`의 직접적인 `requests` 코드에는 **POST 예제가 없습니다.** 따라서 POST 부분은 이 파일 자체의 구현이 아니라 GET과 대비해서 이해해야 하는 개념입니다.

---

# 2. LLM 결과를 JSON으로 만들어 다음 API의 입력으로 사용하는 흐름

이 과제에서 **가장 중요한 학습 포인트**입니다.

전체 구조를 단순화하면 다음과 같습니다.

```text
사용자
  │
  │ --date 2026-09-10
  ▼
① OpenAI LLM
  │
  ▼
② JSON 추천 결과
  │
  │ recommended_city
  ▼
③ Naver 장소검색 API
  │
  ▼
④ 맛집 JSON
  │
  ▼
⑤ OpenAI LLM
  │
  ▼
⑥ 최종 여행 리포트
```

실제 코드를 따라가 보겠습니다.

### ① 사용자가 여행 날짜 입력

```python
travel_date, dry = parse_args()
```

예를 들어:

```text
2026-09-10
```

이 날짜가 `get_recommendation()`으로 전달됩니다.

```python
recommendation = get_recommendation(travel_date)
```

### ② LLM에게 JSON 형태로 결과를 요구

프롬프트에서 아예 출력 구조를 지정합니다.

```python
{
    "recommended_city": "지역명(string)",
    "weather": "해당 시기 일반적 날씨 요약(string)",
    "events": ["행사/축제 후보 1~3개(string)"],
    "reason": "추천 근거 2~4문장(string)"
}
```

따라서 LLM이 예를 들어 다음과 같은 결과를 반환하도록 설계되어 있습니다.

```json
{
  "recommended_city": "제주",
  "weather": "선선하고 여행하기 좋은 날씨",
  "events": ["가을 문화행사"],
  "reason": "가을 풍경과 관광지가 풍부합니다."
}
```

### ③ 문자열을 실제 Python 데이터로 변환

LLM이 반환한 것은 처음에는 **텍스트**입니다.

```python
raw = call_llm(prompt, system=system_prompt)
```

그래서 `extract_json()`을 이용합니다.

```python
data = extract_json(raw)
```

내부에서는:

```python
return json.loads(match.group(0))
```

를 사용합니다.

즉,

**LLM 문자열 → JSON 파싱 → Python `dict`**

으로 바뀝니다.

그 다음 `validate_recommendation()`에서 필수 항목과 데이터 형식도 검사합니다.

```python
REQUIRED_KEYS = [
    "recommended_city",
    "weather",
    "events",
    "reason"
]
```

이 과정이 중요한 이유는 LLM의 자연어 출력을 그대로 다음 시스템에 전달하지 않고 **컴퓨터가 안정적으로 처리할 수 있는 구조화 데이터로 변환**하기 때문입니다.

### ④ JSON의 도시명을 다음 API 입력으로 사용

`main()`에서 핵심 연결이 일어납니다.

```python
recommendation = get_recommendation(travel_date)

restaurants = search_restaurants(
    recommendation.get("recommended_city", "")
)
```

여기서 앞 단계 LLM의

```python
recommendation["recommended_city"]
```

가 다음 단계 장소검색 API의 입력이 됩니다.

예를 들어:

```text
LLM
 ↓
recommended_city = "제주"
 ↓
search_restaurants("제주")
 ↓
query = "제주 맛집"
 ↓
Naver API
```

이것이 과제에서 말하는 **"LLM 출력 결과를 구조화(JSON)하여 다음 단계의 입력으로 활용한다"**의 정확한 의미입니다.

---

# 3. 외부 API 오류와 대응 원칙

`travel_planner.py`에는 오류처리가 비교적 잘 나누어져 있습니다.

대표적으로 다음 네 가지를 이해하면 됩니다.

|오류|의미|travel_planner 대응|
|---|---|---|
|인증 오류|API Key 오류|`401`, `403` 검사|
|쿼터 오류|API 사용량 초과|현재 별도 처리 없음|
|네트워크 오류|연결 실패/Timeout 등|`RequestException`|
|파싱 오류|응답 JSON 등의 형식 문제|`ValueError`, `TypeError`, `KeyError`|

### 인증 오류

```python
if res.status_code in (401, 403):
```

인증에 실패하면:

```python
log_error(
    "place_search",
    "AUTH_ERROR",
    ...
)
```

그리고 프로그램 전체를 중단하지 않고:

```python
return []
```

처리합니다.

그래서 실제 실행할 때 보셨던 것처럼:

```text
오류: 인증 실패(401).
키 값/헤더명을 확인하세요.

맛집 섹션은 '데이터 없음'으로 처리하고
계속 진행합니다.
```

가 가능합니다.

### 네트워크 오류

```python
except requests.exceptions.RequestException as e:
```

서버 접속 실패, timeout, 통신 문제 등이 발생했을 때 처리합니다.

### 파싱/응답 오류

장소검색 결과를 처리하면서:

```python
except (ValueError, TypeError, KeyError) as e:
```

로 잘못된 값이나 예상과 다른 응답 구조에 대응합니다.

LLM 결과에서도 파싱 오류를 별도로 처리합니다.

```python
try:
    raw = call_llm(...)
    data = extract_json(raw)
    return validate_recommendation(data)
except Exception as e:
```

첫 번째 JSON 파싱이 실패하면 **한 번 재시도**합니다.

```python
for attempt in range(2):
```

그래도 실패하면:

```python
log_error(
    "recommendation",
    "PARSE_ERROR",
    str(e)
)
```

를 기록하고 기본값으로 계속 진행합니다.

### 쿼터 오류

여기에는 한 가지 보완점이 있습니다.

현재 코드에는 `401/403` 인증 오류는 명시적으로 처리하지만, API 사용량 초과에 해당할 수 있는 **HTTP 429 등에 대한 별도 분기 처리는 없습니다.**

따라서 과제 설명에서는:

> 인증 오류는 401/403으로 별도 처리하고 있으며, 네트워크 오류와 응답 파싱 오류도 예외처리한다. 다만 쿼터 초과 오류는 현재 코드에서 별도 분류하지 않고 있으므로 향후 HTTP 429 등을 `QUOTA_ERROR`로 처리하도록 개선할 수 있다.

라고 설명하면 정확합니다.

---

# 4. API Key를 `.env`로 관리하는 이유

이 부분 역시 코드에서 명확하게 확인됩니다.

맨 위에:

```python
from dotenv import load_dotenv

load_dotenv()
```

가 있습니다.

그리고 API Key를 코드에 직접 쓰지 않고:

```python
OPENAI_API_KEY = os.getenv("OPENAI_API_KEY")
```

처럼 가져옵니다.

네이버 키도 마찬가지입니다.

```python
NAVER_CLIENT_ID = os.getenv("NAVER_CLIENT_ID")
NAVER_CLIENT_SECRET = os.getenv("NAVER_CLIENT_SECRET")
NAVER_API_URL = os.getenv("NAVER_API_URL")
```

즉 Python 소스에는 실제 API Key를 적지 않습니다.

`.env`에는 예를 들어 다음과 같이 저장합니다.

```text
OPENAI_API_KEY=실제_API_KEY
NAVER_CLIENT_ID=실제_ID
NAVER_CLIENT_SECRET=실제_SECRET
NAVER_API_URL=API_호출_URL
```

이렇게 하는 가장 큰 이유는 **보안과 환경 분리**입니다.

특히 GitHub에 다음과 같은 코드가 올라가면 위험합니다.

```python
OPENAI_API_KEY = "sk-xxxxxxxxxxxxxxxx"
```

반면 `.env`를 `.gitignore`에 등록하면 API Key를 GitHub에 올리지 않고 각 개발환경에서 별도로 관리할 수 있습니다.

따라서 구조는:

```text
.env
 │
 │ load_dotenv()
 ▼
환경변수
 │
 │ os.getenv()
 ▼
travel_planner.py
 │
 ▼
OpenAI / Naver API
```

가 됩니다.

---

# 5. 이 과제의 전체 데이터 흐름

결국 `main()`을 보면 지금까지의 학습내용이 하나로 연결됩니다.

```python
recommendation = get_recommendation(travel_date)

restaurants = search_restaurants(
    recommendation.get("recommended_city", "")
)

report_md = generate_report(
    travel_date,
    recommendation,
    restaurants
)

save_results(
    travel_date,
    recommendation,
    restaurants,
    report_md
)
```

이를 학습 관점에서 정리하면:

```text
사용자 날짜 입력
       ↓
OpenAI API 호출
       ↓
LLM 자연어 출력
       ↓
JSON 추출
extract_json()
       ↓
JSON 검증
validate_recommendation()
       ↓
recommended_city 추출
       ↓
Naver 장소검색 API GET 요청
       ↓
맛집 JSON 응답
       ↓
Python list/dict로 구조화
       ↓
추천정보 + 맛집정보
       ↓
OpenAI API 재호출
       ↓
Markdown 여행 리포트
       ↓
JSON + MD 파일 저장
```

## 과제 발표 때 이렇게 설명하면 됩니다

> **travel_planner는 여러 API를 순차적으로 연결하는 프로그램입니다. 먼저 사용자가 여행 날짜를 입력하면 OpenAI LLM이 여행지를 추천합니다. 이때 LLM의 결과를 단순 자연어가 아니라 recommended_city, weather, events, reason을 가진 JSON 구조로 받아 검증합니다. 그중 recommended_city를 다음 단계인 네이버 장소검색 API의 검색어로 사용합니다. 장소검색 API는 GET 방식으로 호출하며 URL, Header, Parameter를 전달하고 JSON 응답을 받습니다. 외부 API에서 인증 오류나 네트워크 오류, 응답 파싱 오류가 발생해도 프로그램 전체가 중단되지 않도록 예외처리를 했습니다. 또한 OpenAI와 네이버 API Key는 소스코드에 직접 작성하지 않고 `.env`와 환경변수로 관리하여 GitHub 등에 비밀키가 노출되는 것을 방지했습니다.**

이 설명을 이해하고 코드에서 `get_recommendation()` → `extract_json()` → `search_restaurants()` → `generate_report()`의 연결을 설명할 수 있다면, 이번 과제의 핵심 학습목표를 제대로 이해한 것입니다.