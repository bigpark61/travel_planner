
4~9단계 중 특정 단계(예: LLM 프롬프트 설계, argparse 코드, 에러 처리 로직)부터 실제 코드로 같이 작성해줘

질문: LLM API는 어떤 것을 사용하실 건가요? 답변: OpenAI (GPT)

질문: 지도/장소 검색 API는 어떤 것을 사용하실 건가요? 답변: Naver Local Search

두 케이스 모두 요구사항대로 동작합니다 (usage 출력 후 종료 / 키 미설정 시 안내 후 종료). 이제 보조 파일들을 만들고 전달할게요.**4~9단계 코드**를 4~9단계 요구사항에 맞게 작성했고, 문법 검사와 두 가지 케이스(잘못된 날짜 형식 / API 키 미설정)를 직접 실행해서 확인했습니다. 핵심 설계 포인트만 짚어드릴게요.

**4단계 (argparse)**

- `--date` 필수 옵션 + `datetime.strptime`으로 형식 검증 → 실패 시 `parser.print_usage()`로 사용법 출력 후 `sys.exit(1)`

**5단계 (LLM 프롬프트 설계)**

- `system` 메시지에 JSON 스키마를 텍스트로 명시하고 "다른 설명/코드블록 없이 JSON만" 강하게 지시
- `extract_json()`으로 정규식(`\{.*\}`)을 이용해 혹시 섞여 나올 수 있는 코드블록(` ```json `)에서도 JSON만 뽑아냄
- 1차 실패 시 프롬프트를 "필수 키만 다시 JSON으로" 좁혀서 **딱 1회만** 재시도 (요구사항의 무한 재시도 금지 준수)

**6단계 (Naver Local Search 연동)**

- `X-Naver-Client-Id` / `X-Naver-Client-Secret` 헤더 방식
- `401/403` → 인증 실패 로그 + 빈 리스트 반환 (프로그램 중단 없음)
- 결과 0건 → `EMPTY_RESULT` 기록 후 빈 리스트 반환
- 응답의 `title`에 포함된 `<b>` 태그는 정규식으로 제거

**8단계 (에러 처리)**

- 전역 `errors` 리스트에 `{step, type, message}` 형태로 누적 → 최종 리포트에 `append_error_section()`으로 자동 첨부
- 지도 API 실패해도 리포트 생성은 계속 진행 (요구사항 그대로 반영)

남은 것은 **README.md 작성(10단계)**과 **직접 발급받은 API 키로 실제 테스트**입니다. README도 지금 같이 작성해드릴까요, 아니면 먼저 키를 발급받아 실행해보시겠어요?

---
