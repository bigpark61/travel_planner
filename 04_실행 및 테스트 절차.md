
아래는 로컬에서 백엔드(Flask) + 프론트 테스트를 완전히 실행하고 검증하는 단계별 절차입니다. PowerShell(Windows) 명령을 그대로 복사해 실행할 수 있습니다. 두 가지 실행 방식(권장: venv python 직접 사용 / 선택: Activate.ps1로 가상환경 활성화) 모두 포함합니다.

**사전조건**

- 프로젝트 루트: `C:\Users\PC\OneDrive\Obsidian_Vault_G\02_Codyssey\2단계 과제\2A1-2\program`
- 이미 [venv](vscode-file://vscode-app/c:/Program%20Files/Microsoft%20VS%20Code/resources/app/out/vs/code/electron-browser/workbench/workbench.html)가 생성되어 있고, [app.py](vscode-file://vscode-app/c:/Program%20Files/Microsoft%20VS%20Code/resources/app/out/vs/code/electron-browser/workbench/workbench.html)와 [index.html](vscode-file://vscode-app/c:/Program%20Files/Microsoft%20VS%20Code/resources/app/out/vs/code/electron-browser/workbench/workbench.html), [travel_planner.py](vscode-file://vscode-app/c:/Program%20Files/Microsoft%20VS%20Code/resources/app/out/vs/code/electron-browser/workbench/workbench.html)가 존재합니다.
- 네이버 키로 실제 테스트하려면 [.env](vscode-file://vscode-app/c:/Program%20Files/Microsoft%20VS%20Code/resources/app/out/vs/code/electron-browser/workbench/workbench.html)에 [NAVER_CLIENT_ID](vscode-file://vscode-app/c:/Program%20Files/Microsoft%20VS%20Code/resources/app/out/vs/code/electron-browser/workbench/workbench.html) / [NAVER_CLIENT_SECRET](vscode-file://vscode-app/c:/Program%20Files/Microsoft%20VS%20Code/resources/app/out/vs/code/electron-browser/workbench/workbench.html) 를 넣어야 합니다. 테스트(모의)는 [dry_run=1](vscode-file://vscode-app/c:/Program%20Files/Microsoft%20VS%20Code/resources/app/out/vs/code/electron-browser/workbench/workbench.html)로 가능합니다.

**1) (권장) venv의 Python으로 패키지 설치**

-  가상환경 새로만들기
   deactivate '가상환경 종료
    y -0p '설치된 파이선 확인
    cd C:\Temp\travel_planner
    Remove-Item -Recurse -Force .\venv '꺠진 가상환경 venv 삭제
    py -3.14 -m venv venv '가상환경 만들기


-  가상환경 활성화
    Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass '가상환경 활성화
    .\venv\Scripts\Activate.ps1
    python --version '파이선 확인
    where.exe python
    python -m pip install --upgrade pip 
    python -m pip install -r requirements.txt 
- 

> 주의: 시스템의 PowerShell 실행 정책 때문에 `Activate.ps1`가 차단될 수 있습니다. 위의 한시적 우회(`-Scope Process`)는 안전합니다.

**2) .env 준비 (실제 API 테스트용)**

- 새 파일 생성(예시):
    OPENAI_API_KEY=your_openai_api_key
    NAVER_CLIENT_ID=your_naver_client_id
    NAVER_CLIENT_SECRET=your_naver_client_secret
    NAVER_API_URL=https://openapi.naver.com/v1/search/local.json

- 모의(dry-run)만 할 경우 [.env](vscode-file://vscode-app/c:/Program%20Files/Microsoft%20VS%20Code/resources/app/out/vs/code/electron-browser/workbench/workbench.html)는 없어도 됩니다.

현재 올려주신 `app.py`를 기준으로 보면, **CLI 테스트**와 **웹 테스트**는 목적이 조금 다릅니다. CLI는 `travel_planner.py` 자체의 여행추천 흐름을 확인하고, 웹 테스트는 `app.py`의 Flask 서버를 통해 브라우저/API에서 같은 기능이 정상 동작하는지 확인하는 과정입니다. `app.py`에는 `/api/search`, `/api/plan`, `/` 세 경로가 구현되어 있습니다.

## 1. CLI에서 테스트하는 절차

VS Code에서 `travel_planner` 폴더를 연 뒤 터미널을 실행합니다.

```powershell
cd C:\Temp\travel_planner
```

가상환경을 활성화합니다.

```powershell
.\venv\Scripts\Activate.ps1
```

정상 활성화되면 앞에 `(venv)`가 표시됩니다.
오류발생시,
    Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass '가상환경 활성화
    .\venv\Scripts\Activate.ps1

```text
(venv) PS C:\Temp\travel_planner>
```

필요한 패키지가 설치되어 있는지 확인합니다.

```powershell
pip install -r requirements.txt
```

그리고 `.env` 파일에 필요한 API 키가 설정되어 있어야 합니다. 현재 `app.py`는 네이버 관련 값으로 다음 환경변수를 읽습니다.

```text
NCP_APIGW_API_KEY_ID=발급받은_ID
NCP_APIGW_API_KEY=발급받은_KEY
NAVER_API_URL=사용할_API_URL
```

그리고 `travel_planner.py`에서 OpenAI를 사용한다면 해당 파일이 요구하는 OpenAI API 키도 `.env`에 있어야 합니다.

이제 CLI 프로그램을 실행합니다.

```powershell
python travel_planner.py --date 2026-09-20
```

정상이라면 대략 다음 흐름으로 진행됩니다.

```text
[1/3] 1차 추천 생성 중(LLM)...
    - recommended_city: ...

[2/3] 맛집 검색 중(지도/장소 API)...
    - 맛집 검색 완료

[3/3] 최종 리포트 생성 중(LLM)...
    - 리포트 생성 완료

완료!
```

마지막으로 `results` 폴더를 확인합니다.

```text
travel_planner
 ├─ travel_planner.py
 ├─ app.py
 ├─ .env
 ├─ web
 └─ results
      ├─ 2026-09-20_raw.json
      └─ 2026-09-20_travel_plan.md
```

즉, **CLI 테스트의 핵심 성공 기준**은 `travel_planner.py → LLM 추천 → 장소검색 → 최종 보고서 → results 저장`까지 정상적으로 수행되는 것입니다.

---

## 2. 웹에서 테스트하는 절차

웹에서는 `travel_planner.py`를 직접 실행하는 것이 아니라 **`app.py`를 실행**합니다.

먼저 같은 가상환경에서 다음을 실행합니다.

```powershell
python app.py
```

현재 `app.py`는 Flask 서버를 `0.0.0.0`, 포트 `5000`으로 실행하도록 되어 있습니다.

정상이라면 터미널에 비슷하게 표시됩니다.

```text
NAVER API key ID loaded: xxxx***
NAVER API key loaded: xxxx***

 * Running on http://127.0.0.1:5000
 * Debug mode: on
```

여기서 API 키 전체가 아니라 앞부분만 `xxxx***` 형태로 출력되는 것은 정상입니다. 코드에서도 보안을 위해 전체 키를 출력하지 않도록 되어 있습니다.

브라우저를 열고 주소창에 다음 주소를 입력합니다.

```text
http://127.0.0.1:5000
```

`app.py`의 `/` 경로는 `web/index.html`을 반환하도록 되어 있으므로, `web/index.html`이 존재한다면 여행추천 웹 화면이 나타나야 합니다.

웹 화면에서 날짜를 선택하고 여행추천 버튼을 누르면 프론트엔드가 `/api/plan`을 호출하는 구조여야 합니다.

예를 들어 직접 API를 테스트하려면 브라우저 주소창에서도 다음처럼 확인할 수 있습니다.

```text
http://127.0.0.1:5000/api/plan?date=2026-09-20
```

정상이면 JSON 형태로 다음과 같은 데이터가 반환됩니다.

```json
{
  "recommendation": {
    "recommended_city": "..."
  },
  "restaurants": [],
  "report_md": "...",
  "json_path": "...",
  "md_path": "..."
}
```

실제로 `app.py`의 `/api/plan`은 **날짜 입력 → LLM 여행지 추천 → 맛집 검색 → 보고서 생성 → 파일 저장 → JSON 응답** 순서로 처리합니다.

## 3. 장소 검색 API만 따로 테스트

현재 `app.py`에는 네이버 장소 검색을 독립적으로 시험할 수 있는 `/api/search`도 있습니다.

Flask 서버를 켜둔 상태에서 브라우저에 예를 들어 다음처럼 입력합니다.

```text
http://127.0.0.1:5000/api/search?query=제주맛집
```

정상이면 네이버 API에서 받은 JSON이 표시됩니다.

반대로 `.env` 설정이 잘못되면 현재 코드에서는 다음과 같은 오류를 돌려주도록 되어 있습니다.

```json
{
  "error": "NAVER_API_URL not set in .env"
}
```

또는 인증 실패가 발생하면 `error`와 `api_response`가 JSON으로 반환됩니다.

## 4. 전체 테스트 순서

과제 제출 관점에서는 다음 순서로 테스트하면 가장 이해하기 쉽습니다.

```text
① VS Code에서 프로젝트 열기
        ↓
② venv 활성화
        ↓
③ .env API Key 확인
        ↓
④ CLI 테스트
   python travel_planner.py --date 2026-09-20
        ↓
⑤ results/*.json, *.md 확인
        ↓
⑥ 웹 서버 실행
   python app.py
        ↓
⑦ 브라우저
   http://127.0.0.1:5000
        ↓
⑧ 장소 API 단독 테스트
   /api/search?query=제주맛집
        ↓
⑨ 전체 여행추천 API 테스트
   /api/plan?date=2026-09-20
        ↓
⑩ 웹 화면에서 최종 여행추천 테스트
```

핵심 차이는 **CLI는 `travel_planner.py`의 백엔드 로직 자체를 테스트**하는 것이고, **웹은 `app.py`의 Flask API + `web/index.html` 프론트엔드 + `travel_planner.py`를 통합해서 테스트**하는 것입니다.

특히 지금 단계에서는 **CLI가 먼저 정상 작동하는지 확인한 후 `python app.py`로 웹 테스트를 하는 순서**를 권합니다. CLI가 실패하면 웹에서도 같은 백엔드 로직에서 문제가 발생하기 때문에 오류 원인을 찾기가 훨씬 쉽습니다.

**6) 생성된 결과 파일 확인**

- 결과 파일은 [results](vscode-file://vscode-app/c:/Program%20Files/Microsoft%20VS%20Code/resources/app/out/vs/code/electron-browser/workbench/workbench.html) 폴더에 생성됩니다.


**7) 로그 / 오류 디버깅 팁**

- 401/403: 네이버 키 또는 애플리케이션에 등록한 Referer(도메인) 설정을 확인하세요.
- 서버 콘솔(Flask 출력)에 예외 트레이스가 출력됩니다 — 해당 로그를 확인하세요.
- CORS 문제(프론트가 다른 포트에서 호출할 때): [app.py](vscode-file://vscode-app/c:/Program%20Files/Microsoft%20VS%20Code/resources/app/out/vs/code/electron-browser/workbench/workbench.html)에 `CORS(app)`를 사용했지만 운영 시에는 정확한 오리진만 허용하세요.

**8) 안전 관련 주의사항**

- 절대 `Client Secret`을 브라우저 자바스크립트에 하드코딩하지 마세요.
- [.env](vscode-file://vscode-app/c:/Program%20Files/Microsoft%20VS%20Code/resources/app/out/vs/code/electron-browser/workbench/workbench.html)는 민감 정보 포함 — [.gitignore](vscode-file://vscode-app/c:/Program%20Files/Microsoft%20VS%20Code/resources/app/out/vs/code/electron-browser/workbench/workbench.html)에 이미 추가되어 있어야 합니다.
- 운영 시엔 HTTPS, 오리진 제한, 쿼터 모니터링을 적용하세요.

---
